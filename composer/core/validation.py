from typing import Literal

ValidationType = Literal["prover", "natural language requirements", "tests"]

prover : ValidationType = "prover"
reqs : ValidationType = "natural language requirements"
tests : ValidationType = "tests"